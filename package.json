{
  "name": "medikart",
  "version": "1.0.0",
  "private": true,
  "description": "MediKart — Modern pharmacy e-commerce platform for India",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "dev:web": "npm run dev -w frontend",
    "dev:api": "npm run dev -w backend",
    "build": "npm run build -w frontend",
    "start": "concurrently -n web,api -c blue,green \"npm run start -w frontend\" \"npm run start -w backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
